STUDY TRACKER — BUILD OTIMIZADO

Versão atual do projeto: 62.0.0

O que mudou:
1. O build.bat continua aumentando a versão principal automaticamente.
   62.0.0 -> 63.0.0 -> 64.0.0...
2. A versão do Electron vem do package.json.
3. A versão do GitHub Pages/PWA vem de version.json.
4. O build atualiza version.json e o cache do service-worker automaticamente.
5. O Electron Builder gera apenas o instalador NSIS x64.
   O executável portable não é mais criado.
6. Antes de cada build, a pasta dist antiga é apagada.
7. Depois do build, win-unpacked e arquivos de diagnóstico são removidos.

Resultado esperado na pasta dist:
- Study Tracker Setup X.0.0.exe
- Study Tracker Setup X.0.0.exe.blockmap (pequeno; útil para atualização automática)
- latest.yml (pequeno; útil para atualização automática)

O instalador continuará com aproximadamente 70–80 MB.
Isso é normal no Electron porque Chromium e Node acompanham o programa.
A economia vem de não guardar também o portable e o win-unpacked.

IMPORTANTE:
O build atualiza version.json LOCALMENTE.
Para o GitHub Pages mostrar a nova versão, version.json precisa ir junto no commit/push.
A próxima etapa pode automatizar GitHub Releases e atualização do aplicativo instalado.
